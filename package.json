{
  "name": "simple-web",
  "type": "module",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "tailwindcss -i client/tailwind.css -o client/index.css",
    "start": "npm run build:css && node .",
    "dev": "npx nodemon -w client -w server -w index.js -e html,css,js --exec 'npm start'"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "body-parser": "^1.20.3",
    "express": "^4.21.1"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.13"
  }
}
